{
  "name": "qbiq",
  "version": "0.0.1",
  "description": "A qbiq-hono project",
  "scripts": {
    "build": "bun build src/index.ts",
    "dev": "bun run --hot src/index.ts"
  },
  "dependencies": {
    "@hono/oauth-providers": "^0.5.0",
    "bun": "^1.1.7",
    "hono": "^4.3.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
